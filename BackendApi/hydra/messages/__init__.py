# TODO:

# Link to channels to separate messages by namespace/channel
# Persist each message in database (so that users can see messages from before as
# well as messages that occurred while they weren't logged in)
#
